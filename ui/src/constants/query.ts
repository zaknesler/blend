export const QUERY_KEYS = {
  // Feeds
  FEEDS: 'feeds.index',
  FEEDS_ADD: 'feeds.add',
  FEEDS_VIEW: 'feed.view',

  // Entries
  ENTRIES_INDEX: 'entries.index',
  ENTRIES_VIEW: 'entries.view',
} as const;
