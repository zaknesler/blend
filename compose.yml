services:
  blend:
    image: ghcr.io/zaknesler/blend:latest
    container_name: blend
    restart: unless-stopped
    ports:
      - "4000:4000"
    volumes:
      - ./data:/data
    environment:
      - BLEND_WEB_HOST=0.0.0.0
      - BLEND_CRYPTO_JWT_SIGNING_KEY=
